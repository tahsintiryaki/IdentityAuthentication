@model IEnumerable<Identity.Models.Authentication.AppRole>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordDate)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordDate)
            </td>
           
            <td>
        
                <a asp-action="#" asp-route-id="@item.Id">Details</a> |
                <a asp-action="#" asp-route-id="@item.Id">Delete</a>
                <a asp-action="AssignAuthority" asp-route-id="@item.Id">Yetki Düzenle</a>
            </td>
        </tr>
}
    </tbody>
</table>
